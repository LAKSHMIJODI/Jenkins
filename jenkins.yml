---
- hosts: all
  become: yes
  tasks:
  - name: install tree
    apt:
      name: tree
      state: present
      groups: WebServer
  - name: install tomcat
    apt:
      name: tomcat7
      state: present
      groups: Ubuntu
